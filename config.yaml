# LastZ Automation — Alliance Gifts
# Edit thresholds/intervals here; no code changes needed for normal tuning.

game:
  process_name: Survival.exe   # Activity Monitor / CrossOver process name

paths:
  templates_dir: templates/active
  logs_dir: logs

thresholds:
  claim_all: 0.82
  claim_button: 0.72
  alliance_shield: 0.7
  alliance_gifts: 0.7
  rare_tab: 0.65
  hq_world_button: 0.7         # watcher: detect HQ mode to return to wilderness

watcher:
  alliance_interval_sec: 180   # seconds between claim runs in menu option 2

coordinates:
  # Logical offset from game window top-left — empty map click to dismiss overlays
  dismiss_outside:
  - 100
  - 300
